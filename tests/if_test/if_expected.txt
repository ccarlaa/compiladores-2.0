inteiro principal() {
    real pi = 3.14;
    caractere letra = 'a';
    /* Isso aqui vira comentário de portugol */
    // Isso é apenas um comentário comum
    se (pi > 3) {
        retorne 1;
    }
    retorne 0;
}
